# Sao chép file này thành .env.local rồi đổi mật khẩu trước khi chạy thật.
ADMIN_USER=vipextoy
ADMIN_PASSWORD=doi-mat-khau-nay

# Bắt buộc nếu deploy lên Vercel — nếu không, đơn hàng đặt trên web sẽ
# KHÔNG được lưu ở đâu cả. Xem hướng dẫn lấy key trong src/lib/notify-order.ts
RESEND_API_KEY=
ORDER_NOTIFY_EMAIL=
